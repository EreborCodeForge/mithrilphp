#!/usr/bin/env php
<?php

declare(strict_types=1);

use Erebor\Mithril\Console\Kernel;

require __DIR__ . '/../../../autoload.php';

$kernel = new Kernel();
$status = $kernel->handle($argv);

exit($status);
